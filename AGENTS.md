# 项目协作规则

本文件适用于整个仓库以及由本仓库创建的所有开发分支。

## 沟通要求

- 面向编程初学者说明实现结果，优先使用中文和容易理解的业务逻辑。
- 大量编码完成后，必须说明“输入是什么、经过什么处理、保存或输出什么”。
- 明确区分已经完成的功能、仅搭建的骨架和暂未实现的功能。

## 验证要求

- 大量编码或跨模块改动后，必须重新执行 Release 编译和完整测试，不能只依赖代码阅读。
- 本机标准验证命令为：

```powershell
F:\Qt\6.8.3\msvc2022_64\bin\qt-cmake.bat -S . -B build-local -DBUILD_TESTING=ON
cmake --build build-local --config Release
ctest --test-dir build-local -C Release --output-on-failure
```

- 如果因为环境原因不能运行某项测试，必须明确说明未验证的内容和原因。
- 每个并行分支先运行本分支测试；合并到主分支后再运行完整测试。

## 文件与数据安全

- 大量删除、重建数据库、覆盖大批文件或清除用户数据前，必须先询问用户意见，不能自动执行。
- 不提交 `build-local`、`out`、临时数据库、真实密码或本地私密配置。
- 已执行过的数据库迁移不得修改；需要调整结构时新增更高版本的迁移文件。
- 不覆盖与当前任务无关的用户改动。
