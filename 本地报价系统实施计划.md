# 本地单机报价管理系统实施计划

## 目标

先完成一台 Windows 电脑上的报价闭环：物料、客户、BOM、报价核算、保存查询和 Excel 导出。

## 实施顺序

1. 建立 Qt 6 / CMake / C++17 工程，划分桌面端、API 服务、领域模型、数据库迁移与测试模块。
2. 安装本地 MySQL 8.4，创建用户、物料、客户、报价单与报价明细等基础表。
3. 实现本地 REST API：登录、权限、物料管理、客户管理、报价创建与保存。
4. 实现平铺 BOM 和统一核算：
   - 材料成本 = 数量 × 单价快照之和
   - 未税报价 = (材料成本 + 运费 + 其他费用) × (1 + 加价率)
   - 含税报价 = 未税报价 × (1 + 税率)
5. 实现 Qt 界面：登录、物料库、客户库、报价列表和报价编辑页。
6. 支持单张报价导出 Excel，并完成核心流程测试。

## 固定约束

- 初版的 MySQL、C++ API 和 Qt 客户端均在同一台电脑运行，仅使用 `localhost`。
- 使用 Qt Widgets、Qt HttpServer 和 MySQL 8.4。
- 角色仅保留管理员、报价员、查看员。
- 报价状态仅保留草稿、已报价、已作废。
- 报价保存物料名称、规格、单位与单价快照；后续物料调价不影响历史报价。
- 已被报价引用的物料只能停用，不能删除。

## 首版不做

云端数据库、两地同步、VPN、审批流、层级 BOM、统计报表、批量 Excel 导入、自动备份和安装包。
