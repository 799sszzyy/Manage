# 第四批（Batch 4）报价生命周期验收计划

## 固定标号

- 批次：Batch 4
- 软件版本：v0.4.0
- 并行验收端口：18084
- 正式默认端口：18080
- 整合分支：`codex/batch4-quote-integration-v0.4.0-p18084`

本文件中的 `18084` 只用于开发验收，不改变正式运行默认端口。

## 自动化验收

1. Release 配置和编译成功。
2. 保持 Batch 1～3 的全部测试通过。
3. 报价数据测试覆盖事务、核算、快照、乐观锁、状态流转、复制和草稿删除。
4. 报价路由测试覆盖权限、JSON、HTTP 状态和全部生命周期动作。
5. 报价桌面测试覆盖管理员、报价员、查看员、临时密码和中文错误提示。

## 真实 MySQL 验收

必须创建名称以 `_test` 结尾的全新数据库，并在 `18084` 执行以下顺序：

1. 启动服务，确认 001 和 002 迁移完整执行。
2. 初始化管理员、登录并修改临时密码。
3. 创建客户、物料与 BOM。
4. 创建报价草稿，保存客户与物料快照并核对金额。
5. 重启服务，确认草稿仍可查询和修改。
6. 使用旧 `revision` 修改，确认返回 409；使用新版本修改成功。
7. 发布报价，确认内容被冻结且记录发布时间。
8. 修改原客户、物料名称和单价，再读取已发布报价，确认历史快照未改变。
9. 复制已发布报价，确认生成新编号、新草稿和 `sourceQuoteId`。
10. 作废原报价，确认只能查询和复制。
11. 新建另一个草稿并删除，确认详情返回 404；发布后的报价不能删除。
12. 使用查看员确认只能读取；使用报价员确认可以完成报价业务操作。

## 桌面手工验收

1. 报价列表能按关键词、状态和页码查询。
2. 详情页正确显示编号、客户快照、状态、费用、税率、合计和物料快照。
3. 新建与修改草稿时可选择客户、可选 BOM 和多条物料。
4. 发布、作废、复制、删除按钮只在合法状态和角色下启用。
5. 删除草稿前必须二次确认。
6. 409、403、断网和非法输入均显示可理解的中文信息。

## 第四批边界

第四批完成报价单的数据库生命周期，但不包含 Excel 导出、用户账号管理和统计报表。这三项保留给 Batch 5。

## 2026-08-05 实际验收结果

- 三个永久功能分支分别完成 Release 全量测试：数据层 `20/20`、HTTP API `19/19`、Qt 桌面 `19/19`。
- 按数据层、API、Qt 顺序合并，每一步都重新构建；最终整合分支 `22/22` 全部通过。
- 报价桌面组件曾暴露两个测试请求捕获竞态，修正为按 POST/DELETE 分别记录后连续重复 `10/10` 通过，再次全量回归 `22/22` 通过。
- 在 MySQL 8.4 的全新数据库 `manage_batch4_20260805230320_test` 和端口 `18084` 完成真实黑盒流程：001/002 迁移、管理员初始化和改密、客户/物料/BOM、报价保存、服务重启、旧版本冲突、修改、发布、源资料修改后的快照冻结、复制、作废和草稿删除全部通过。
- Qt 主程序使用 `--api-url http://127.0.0.1:18084 --smoke-test` 对正在运行的真实服务启动成功，正式默认端口仍为 `18080`。
- `viewer` 只读、`quoter` 可写和临时密码门禁由 HTTP 路由测试与 Qt 模拟 HTTP 测试覆盖。当前版本尚无用户账号管理接口，因此真实 MySQL 黑盒流程使用管理员账号；账号管理属于 Batch 5。
