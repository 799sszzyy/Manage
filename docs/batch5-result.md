# 第五批完成记录

- 批次：Batch 5
- 版本：v0.5.0
- 验收端口：18085
- 状态：四个模块已完成并通过验收

## 完成模块

1. 用户与权限管理：管理员可查询、创建、编辑、启停用户和重置临时密码；角色为管理员、报价员、查看员。
2. Excel 与批量处理：支持物料模板、物料导入导出、BOM 导出、报价导出和统计导出，文件格式为 `.xlsx`。
3. 统计分析：支持日期、客户、报价状态筛选，输出汇总、月份、客户和物料分类统计。
4. BOM 拖拽编辑：支持物料拖入、重复物料数量合并、整行排序和行号自动重排。

报价流程保持 `draft -> issued -> void`，本批次没有增加报价审核状态，也没有增加多层 BOM。

## 验收结果

- 四条功能分支均完成 Release 编译和针对性测试。
- 集成分支 Release 全量编译成功，CTest `31/31` 通过。
- 独立数据库 `manage_batch5_test_20260805` 的真实 MySQL/HTTP 链路通过：登录、用户管理、批量预览与提交、无效批次整批拒绝、客户、报价发布、统计查询。
- 四条功能分支、集成分支、构建目录和测试数据库均保留。
